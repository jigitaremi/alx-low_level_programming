#include <stdio.h>

/**
 * mul - function to multiply two integers
 * @a: first number
 * @b: second number
 *
 * Return: result of the multiplication
 */
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
