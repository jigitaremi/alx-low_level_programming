#include <stdio.h>
#include "main.h"
#include <stdlib.h>

/**
 * _abs - function to print absolute of an integer
 * @n: the number to be checked
 *
 * Return: 0 if success
 */
int _abs(int n)
{
	int i = (abs(n));

	return (i);
}
